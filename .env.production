# Public client config for the "maquinita" Supabase project.
# These are NEXT_PUBLIC_ values — they are shipped to the browser by design and
# are safe to commit. The security boundary is Row Level Security, not secrecy
# of these keys. The service role key is NOT here and must never be committed.
NEXT_PUBLIC_SUPABASE_URL=https://ixvxunclnbzguugiqelq.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_TxF0W3VGBPweWc0k5alwWg_ZmaRFeAJ
